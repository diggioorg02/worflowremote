name: My Target Workflow

on:
  repository_dispatch:
    types: [trigger-workflow]
    inputs:
      artifactURL:
        required: true
    
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
